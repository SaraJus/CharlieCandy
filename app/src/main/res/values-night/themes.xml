<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here -->
        <item name="colorPrimary">@color/clean_red_dark</item>
        <item name="colorPrimaryContainer">@color/clean_red_primary</item>
        <item name="colorSecondary">@color/clean_gray_dark</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/red</item>
        <item name="android:windowLightStatusBar">false</item> <!-- Ícones claros na StatusBar -->
    </style>
</resources>
